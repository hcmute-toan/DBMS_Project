<Window x:Class="FinalProject.Views.WarehouseView"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       Title="Warehouse View" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Input Fields -->
            <RowDefinition Height="Auto"/>
            <!-- Buttons -->
            <RowDefinition Height="*"/>
            <!-- DataGrid -->
        </Grid.RowDefinitions>

        <!-- Input Fields -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="1.5*"/>
            </Grid.ColumnDefinitions>

            <!-- Tên kho -->
            <StackPanel Grid.Column="0">
                <TextBlock Text="Tên kho" FontWeight="Bold"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
            </StackPanel>

            <!-- Địa chỉ -->
            <StackPanel Grid.Column="1">
                <TextBlock Text="Địa chỉ" FontWeight="Bold"/>
                <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
            </StackPanel>

            <!-- Sức chứa -->
            <StackPanel Grid.Column="2">
                <TextBlock Text="Slot" FontWeight="Bold"/>
                <TextBox Text="{Binding Slot, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
            </StackPanel>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,10">
            <Button Content="Thêm" Width="80" Margin="5" Command="{Binding AddCommand}" />
            <Button Content="Xóa" Width="80" Margin="5" Command="{Binding DeleteCommand}" />
            <Button Content="Sửa" Width="80" Margin="5" Command="{Binding EditCommand}" />
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding WarehouseList}"
                  SelectedItem="{Binding SelectedWarehouse}"
                  AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding WarehouseId}" Width="Auto"/>
                <DataGridTextColumn Header="Tên kho" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Địa chỉ" Binding="{Binding Address}" Width="*"/>
                <DataGridTextColumn Header="Slot" Binding="{Binding Slot}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
