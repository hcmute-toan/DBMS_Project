<UserControl x:Class="FinalProject.Views.UserView"  
   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"  
   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"  
   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"  
   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
   mc:Ignorable="d"  
   d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Input Fields -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Username -->
            <StackPanel Grid.Column="0">
                <TextBlock Text="Tên đăng nhập" FontWeight="Bold"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
            </StackPanel>

            <!-- Password -->
            <StackPanel Grid.Column="1">
                <TextBlock Text="Mật khẩu" FontWeight="Bold"/>
                <PasswordBox Margin="0,5"/>
            </StackPanel>

            <!-- Role -->
            <StackPanel Grid.Column="2">
                <TextBlock Text="Vai trò" FontWeight="Bold"/>
                <TextBox Text="{Binding Role, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
            </StackPanel>

            <!-- Số điện thoại -->
            <StackPanel Grid.Column="3">
                <TextBlock Text="Số điện thoại" FontWeight="Bold"/>
                <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
            </StackPanel>

            <!-- Địa chỉ -->
            <StackPanel Grid.Column="0" Grid.Row="1">
                <TextBlock Text="Địa chỉ" FontWeight="Bold"/>
                <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
            </StackPanel>

            <!-- Email -->
            <StackPanel Grid.Column="1" Grid.Row="1">
                <TextBlock Text="Email" FontWeight="Bold"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
            </StackPanel>

            <!-- Ảnh -->
            <StackPanel Grid.Column="2" Grid.Row="1">
                <TextBlock Text="Ảnh (link)" FontWeight="Bold"/>
                <TextBox Text="{Binding Img, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
            </StackPanel>

            <!-- Ngày tạo -->
            <StackPanel Grid.Column="3" Grid.Row="1">
                <TextBlock Text="Ngày tạo" FontWeight="Bold"/>
                <DatePicker SelectedDate="{Binding CreatedAt, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
            </StackPanel>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,10">
            <Button Content="Thêm" Width="80" Margin="5" Command="{Binding AddCommand}" />
            <Button Content="Xóa" Width="80" Margin="5" Command="{Binding DeleteCommand}" />
            <Button Content="Sửa" Width="80" Margin="5" Command="{Binding EditCommand}" />
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding UserList}"
                  SelectedItem="{Binding SelectedUser}"
                  AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding UserId}" Width="Auto"/>
                <DataGridTextColumn Header="Tên đăng nhập" Binding="{Binding Username}" Width="*"/>
                <DataGridTextColumn Header="Vai trò" Binding="{Binding Role}" Width="*"/>
                <DataGridTextColumn Header="SĐT" Binding="{Binding Phone}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Địa chỉ" Binding="{Binding Address}" Width="*"/>
                <DataGridTextColumn Header="Ngày tạo" Binding="{Binding CreatedAt}" Width="*"/>
                <DataGridTextColumn Header="Ảnh" Binding="{Binding Img}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
