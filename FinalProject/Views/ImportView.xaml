    <Window x:Class="FinalProject.Views.ImportView"  
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"  
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"  
           Title="ImportView" Height="450" Width="800">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Nhập liệu -->
                <RowDefinition Height="Auto"/>
                <!-- Nút -->
                <RowDefinition Height="*"/>
                <!-- DataGrid -->
            </Grid.RowDefinitions>

            <!-- Nhập liệu -->
            <Grid Grid.Row="0" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <!-- Ngày nhập -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Ngày nhập" FontWeight="Bold"/>
                    <DatePicker SelectedDate="{Binding ImportDate, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
                </StackPanel>

                <!-- Tổng tiền -->
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Tổng tiền" FontWeight="Bold"/>
                    <TextBox Text="{Binding TotalAmount, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
                </StackPanel>

                <!-- Nhà cung cấp -->
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Nhà cung cấp" FontWeight="Bold"/>
                    <ComboBox ItemsSource="{Binding SupplierList}" 
                              SelectedValue="{Binding SupplierId, UpdateSourceTrigger=PropertyChanged}"
                              DisplayMemberPath="SupplierName"
                              SelectedValuePath="SupplierId"
                              Margin="0,5"/>
                </StackPanel>

                <!-- Kho -->
                <StackPanel Grid.Column="3">
                    <TextBlock Text="Kho" FontWeight="Bold"/>
                    <ComboBox ItemsSource="{Binding WarehouseList}" 
                              SelectedValue="{Binding WarehouseId, UpdateSourceTrigger=PropertyChanged}"
                              DisplayMemberPath="WarehouseName"
                              SelectedValuePath="WarehouseId"
                              Margin="0,5"/>
                </StackPanel>
            </Grid>

            <!-- Buttons -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,10">
                <Button Content="Thêm" Width="80" Margin="5" Command="{Binding AddCommand}" />
                <Button Content="Xóa" Width="80" Margin="5" Command="{Binding DeleteCommand}" />
                <Button Content="Sửa" Width="80" Margin="5" Command="{Binding EditCommand}" />
            </StackPanel>

            <!-- DataGrid -->
            <DataGrid Grid.Row="2" ItemsSource="{Binding ImportList}"
                      SelectedItem="{Binding SelectedImport}"
                      AutoGenerateColumns="False" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã nhập" Binding="{Binding ImportId}" Width="Auto"/>
                    <DataGridTextColumn Header="Ngày nhập" Binding="{Binding ImportDate}" Width="*"/>
                    <DataGridTextColumn Header="Tổng tiền" Binding="{Binding TotalAmount}" Width="*"/>
                    <DataGridTextColumn Header="Nhà cung cấp" Binding="{Binding Supplier.SupplierName}" Width="*"/>
                    <DataGridTextColumn Header="Kho" Binding="{Binding Warehouse.WarehouseName}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Window>
