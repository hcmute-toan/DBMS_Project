<UserControl x:Class="FinalProject.Views.ProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">

    <Grid>
        <Border BorderBrush="Black" BorderThickness="1" Margin="10" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Input Fields -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Tên sản phẩm -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Tên sản phẩm" FontWeight="Bold"/>
                        <TextBox Text="{Binding ProductName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
                    </StackPanel>

                    <!-- Mô tả -->
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Mô tả" FontWeight="Bold"/>
                        <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
                    </StackPanel>

                    <!-- Thương hiệu -->
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Thương hiệu" FontWeight="Bold"/>
                        <TextBox Text="{Binding Brand, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
                    </StackPanel>

                    <!-- EAN13 -->
                    <StackPanel Grid.Column="3">
                        <TextBlock Text="Mã EAN13" FontWeight="Bold"/>
                        <TextBox Text="{Binding EAN13, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
                    </StackPanel>
                </Grid>

                <!-- Dòng tiếp theo -->
                <Grid Grid.Row="1" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Giá nhập -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Giá nhập" FontWeight="Bold"/>
                        <TextBox Text="{Binding ImportPrice, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
                    </StackPanel>

                    <!-- Giá sỉ -->
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Giá sỉ" FontWeight="Bold"/>
                        <TextBox Text="{Binding WholesalePrice, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
                    </StackPanel>

                    <!-- Giá bán -->
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Giá bán" FontWeight="Bold"/>
                        <TextBox Text="{Binding SellPrice, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
                    </StackPanel>

                    <!-- Trạng thái -->
                    <StackPanel Grid.Column="3">
                        <TextBlock Text="Trạng thái" FontWeight="Bold"/>
                        <CheckBox Content="Còn hàng" IsChecked="{Binding Status, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
                    </StackPanel>
                </Grid>

                <!-- Dòng cuối cho ảnh và kho -->
                <StackPanel Grid.Row="2" Margin="0,0,0,10" Orientation="Horizontal">
                    <!-- Ảnh -->
                    <StackPanel Width="200">
                        <TextBlock Text="Đường dẫn ảnh" FontWeight="Bold"/>
                        <TextBox Text="{Binding Img, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
                    </StackPanel>

                    <!-- Mã kho -->
                    <StackPanel Width="200" Margin="20,0,0,0">
                        <TextBlock Text="Mã kho" FontWeight="Bold"/>
                        <TextBox Text="{Binding WarehouseId, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
                    </StackPanel>

                    <!-- Các nút -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,20,0,0">
                        <Button Content="Thêm" Width="70" Margin="5" Command="{Binding AddCommand}" />
                        <Button Content="Xóa" Width="70" Margin="5" Command="{Binding DeleteCommand}" />
                        <Button Content="Sửa" Width="70" Margin="5" Command="{Binding EditCommand}" />
                    </StackPanel>
                </StackPanel>

                <!-- DataGrid -->
                <DataGrid Grid.Row="3" ItemsSource="{Binding ProductList}"
                          SelectedItem="{Binding SelectedProduct}"
                          AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,10,0,0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding ProductId}" Width="Auto"/>
                        <DataGridTextColumn Header="Tên sản phẩm" Binding="{Binding ProductName}" Width="*"/>
                        <DataGridTextColumn Header="Mô tả" Binding="{Binding Description}" Width="*"/>
                        <DataGridTextColumn Header="Thương hiệu" Binding="{Binding Brand}" Width="*"/>
                        <DataGridTextColumn Header="EAN13" Binding="{Binding EAN13}" Width="*"/>
                        <DataGridTextColumn Header="Giá nhập" Binding="{Binding ImportPrice}" Width="*"/>
                        <DataGridTextColumn Header="Giá sỉ" Binding="{Binding WholesalePrice}" Width="*"/>
                        <DataGridTextColumn Header="Giá bán" Binding="{Binding SellPrice}" Width="*"/>
                        <DataGridCheckBoxColumn Header="Còn hàng" Binding="{Binding Status}" Width="*"/>
                        <DataGridTextColumn Header="Ảnh" Binding="{Binding Img}" Width="*"/>
                        <DataGridTextColumn Header="Mã kho" Binding="{Binding WarehouseId}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
